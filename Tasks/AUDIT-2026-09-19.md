# Audit repair packet, 2026-09-19

Authorization: Lucas requested an audit of AnimeRPG and AnimeRPG-UE5 and fixes in GitHub.
Scope: verification and documentation repair only. No new gameplay, engine migration,
asset import, release, local scheduler, or change to a registered Windows worktree.

Use a dedicated GitHub audit branch based on director-context commit
`a13c3dae28bf1745d4c13bd48269c07fd3318fc6`, targeting `main`.
The connector cannot access the registered Windows worktree. Local validation uses
an isolated, explicitly limited fixture, not a claimed clone or native acceptance run.
Native acceptance must occur in an owned worktree before merge approval.

## Reproduced defects and allowed paths

- Release assertions are removed by NDEBUG, including side-effecting test calls:
  `CMakeLists.txt`, `cmake/AstralTestSafety.cmake`, `Tests/AssertionsEnabled.cpp`,
  `Scripts/test_test_safety.py`.
- GUI smokes share desktop focus and lack CTest serialization and deadlines:
  the same CMake files. Do not modify their gameplay expectations.
- PowerShell verifier failures can be overwritten by later successful commands:
  `.github/workflows/windows-ci.yml`.
- Main-branch guidance omits the September 17 engine-first direction:
  `AGENTS.md`, `README.md`, `GAME_DEVELOPMENT_CONTROL.md`,
  `Docs/Planning/MILESTONES.md`, `Docs/Project-Status.md`.
- Evidence: this packet and `Docs/QA/AUDIT-2026-09-19.md`.

Preserve the three director-context files inherited from the base branch. Do not
claim that its reference to completed R0 is independent evidence of completion.
Do not edit the local heartbeat or historical QA results to manufacture acceptance.

## Gates

1. `python Scripts/test_test_safety.py`: use real CMake and the available compiler
   to prove assertion evaluation in optimized tests, no change to product Release
   flags, the compile-time guard, and serialized bounded smoke registration.
2. Hosted Windows Debug/Release build, deterministic CTest, static verifiers, and
   clean tracked-tree checks. Record results for the exact PR SHA.
3. Native GUI smokes on one owned interactive Windows desktop, followed by
   independent review. These are pending, not replaced by Linux fixture tests.
4. No merge, release, or game-content restart without Lucas's approval.

Stop at a deterministic failure; preserve the log and open a scoped follow-up.
