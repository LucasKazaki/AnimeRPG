# M9 — Landmark Interaction

Add an offline deterministic interaction layer to the M5 wireframe world: proximity detection for the three existing landmark proxies, an `E` interact action, a bounded discovery ledger, visible selected/visited state, and one interaction-driven reward that routes through existing player combat/resource state without bypassing rules. Include focused unit tests and native runtime smoke. Preserve M4/M7/M8 behavior. No dependencies/downloads. Require Debug/Release all CTest, smoke, diff/scope checks before commit/merge.
