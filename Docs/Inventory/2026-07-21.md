# Toolchain and Workspace Inventory

Date: 2026-07-21

## Workspace

- `C:/AI/projects/AnimeRPG`: not previously a Git repository; contained only rules, task README, and handoff before M1.
- Unreal project: not found (`*.uproject` and Build.cs absent).
- Custom-engine source: now initialized for M1 under `Engine/`, `Game/`, `Tests/`, `Scripts/`.
- Free space: approximately 289 GB on C: (476 GB total, 187 GB used), from `df -h /c`.

## Tool state

| Component | State | Evidence |
|---|---|---|
| Git | installed/verified | `git version 2.54.0.windows.1` |
| Git LFS | installed/verified | `git-lfs/3.7.1` |
| Python | installed/verified | Hermes Python 3.11.15; static gate executed |
| Node/npm | installed/verified | Node v22.23.1; npm 10.9.8 |
| uv | installed/verified | uv 0.11.30 |
| CMake | not installed | `where cmake.exe` returned no path |
| Native C++ compiler | not installed/configured | `where cl.exe`, clang++, and g++ returned no compiler; no Visual Studio compiler found |
| Windows SDK | present, not sufficient | `C:/Program Files/Windows Kits/10` exists; compiler tools not found |
| Unreal Engine | not installed/configured/verified for this workspace | no executable/path or project found |
| Claude Code | installed/verified, auth not independently checked | `claude --version` => 2.1.212 |
| Codex CLI | not installed on PATH | `codex` command not found; Hermes doctor reports OpenAI Codex auth logged in through Hermes |
| Hermes | installed/configured/verified | Hermes v0.19.0; `hermes doctor` passed core environment and reported auth/config state |
| Bionic local model API | running/verified | `GET http://127.0.0.1:1234/v1/models` returned 10 model IDs |
| Agent Studio | installed locally, not launched for this task | package manifest and node_modules present under `C:/AI/projects/LucasAgentStudio` |

## Safety/approval notes

No plugin, paid service, external API, credential, firewall, license acceptance, destructive cleanup, or public deployment was performed. Bionic was queried only on localhost for readiness evidence.

## Blocker

The first runtime build cannot be verified until CMake and a native Windows C++ compiler are available. Installing a compiler may require accepting vendor licenses/terms, which is outside the current mandate. Recommended next action: Lucas explicitly approves a toolchain installation path (for example Visual Studio Build Tools or LLVM), then the build agent reruns the M1 matrix.
