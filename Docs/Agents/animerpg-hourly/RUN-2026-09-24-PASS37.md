# AnimeRPG GAME pass 37 receipt

Pass: `GAME-PASS37-SHADOW-CRYPT-MISSION-PLANNING-2026-09-24`  
Owner: `animerpg-game-hourly`  
Baseline: `02bca71b2da473626578f89b9876afb4045f0de0`  
Branch: `game/pass37-shadow-crypt-mission-planning`  
Target: `main`

## Bounded implementation

- `GAME-182`: reversible Standard/Focused Shadow Crypt mission guidance. Focused mode prioritizes the main Rift Nave stabilization objective without disabling the optional Cooling Cache; the live `LandmarkInteraction` owner exposes the mode switch.
- `GAME-183`: read-only entry-prerequisite report with authoritative Shadow Crypt lead progress plus protagonist/run/checkpoint/replay blockers through the production owner.
- `GAME-184`: read-only next-action guidance derived from mission, field-guide and protagonist authority without changing the player's currently tracked operation.
- `GAME-185`: latest-clear versus personal-best comparison with a bounded non-negative score gap and new-best witness.
- `GAME-186`: bounded four-clear newest-first completion archive, independent of the monotonic personal best.
- `QOL-038`: pre-entry room/objective and Rift Warden encounter preview using already-authored mission facts only, exposed through the production owner before commitment.

Production paths changed:
- `Engine/Scene/ShadowCryptMission.h`
- `Engine/Scene/LandmarkInteraction.h`, thin pass-37 Shadow Crypt planning/record wrappers only

Registered regression paths changed:
- `Tests/ShadowCryptMissionPlanningPass37Tests.inc`
- registration-only include/call additions in `Tests/ThoughtCommandsTests.cpp`

No renderer, platform, editor, import, animation, audio, physics, CMake, workflow, dependency, networking, R0, release, deployment, or other-worker PR path changed.

The new APIs are production game-domain behavior, but this packet intentionally does not add Win32/menu/controller presentation. Native player-playable verification therefore remains separate and is not claimed.

## Research provenance

Primary current official source: HoYoverse Genshin Impact Version 7.1 update details, published 2026-09-23 and accessed 2026-09-24: https://genshin.hoyoverse.com/tr/news/detail/166383 . The packet adapts explicit quest unlock criteria, Focused Experience Mode, guide-style quest direction, historical-best result presentation, and an All Records surface into original Shadow Crypt mission behavior.

Community context for `QOL-038`: `Domain QoL Improvements We Need`, Genshin Impact subreddit, dated 2025-04-22 and accessed 2026-09-24: https://www.reddit.com/r/Genshin_Impact/comments/1k59yyr . A reply requests seeing domain enemies from the character-selection screen before committing. Other replies disagree on related popup/timer changes. This is an anecdotal preference, not consensus, and current resolution of that exact request is unverified.

## Regression coverage authored

The registered pass-37 slice covers:
- 0/2 and 1/2 Shadow Crypt lead prerequisites, ready entry, active-run, checkpoint, and completed-replay states;
- production missing-protagonist rejection followed by exact ready/Enter behavior after a protagonist owner is bound;
- exact next-step guidance and proof that guidance does not mutate the caller's tracked field operation;
- live-owner pre-entry preview of four objective requirements, optional-cache availability, and the Rift Warden finale without starting a run;
- production-owner Standard versus Focused Rift Nave guidance, manual optional-cache availability in Focused mode, return to Standard, repeated-mode no-op, and invalid enum rejection;
- first Gold clear latest/best equality, worse replay best retention, exact score gap, checkpoint resume, and replay state transitions;
- five total accepted clears proving a four-record newest-first archive, out-of-range invalid read, and retained personal best after its original clear falls out of the bounded archive.

## Independent review repairs

Initial Codex review completed against candidate `49113d2a4ef41ac1fd09d1acb9a393b7e2df0812` and identified two material P2 integration gaps:

1. Focused guidance existed only on the private `ShadowCryptMission`, so the production `LandmarkInteraction` owner could not select it. The repair adds the narrow `SetShadowCryptFocusMode` live-owner route and a registered regression that reaches Rift Nave through the production owner, switches to Focused, verifies main-objective guidance, confirms the optional cache is still available, and switches back to Standard.
2. The direct mission entry report did not know about the production owner's required `CharacterProgression`, so it could advertise `EnterShadowCrypt` in a state the live owner would reject. The repair adds protagonist-aware `ShadowCryptEntryStatus` and `ShadowCryptEntryNextAction` wrappers; registered coverage verifies no progression yields `ProtagonistUnavailable`/`BindProtagonist`, then binding progression yields ready/Enter and a successful production `BeginShadowCrypt`.

Those repairs make the earlier candidate's successful hosted workflows and its review evidence stale. Fresh exact-final-head hosted gates and a fresh exact-final-head independent review are required before merge.

## Verification state at record freeze

Hosted Windows Debug/Release and Release-manifest workflows must run against the exact final PR head after all source/record commits. Fresh independent Codex review must also bind to that exact head. Any material finding requires repair and makes earlier evidence stale.

Counts before exact-head acceptance:
- researched reference mechanics: 5
- researched community improvements: 1
- implemented increments: 6
- explicit registered-regression-covered increments: 6
- production game-domain integrated increments: 6
- exact-head hosted verified: 0 pending
- native player-playable verified: 0 not claimed
- independently reviewed clean on final head: 0 pending
- merged: 0 pending

Next action: freeze the compact state on the repaired head, reply to and resolve the repaired review threads, require exact-head hosted Windows/release gates and fresh independent review, repair/rerun any new finding, then re-read `main` and the accepted PR head and merge with an expected-head guard only if every applicable gate is clean.
