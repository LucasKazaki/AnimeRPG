# AnimeRPG Game Worker Run Receipt: Pass 25

Date: 2026-09-23
Owner: `animerpg-game-hourly`
Packet: `Tasks/GAME-PASS25-SHADOWBLADE-TRAINING-HUB-2026-09-23.md`
Base main at pass start: `4c3308051c910b66dd0aebcf513956926beab505`
Branch: `game/2026-09-23-training-hub-pass25`
Target: `main`
PR: `#44`

## Bounded result

This pass integrates the pass-24 Shadowblade training coach into the existing live Lincoln `LandmarkEncounter` flow while leaving engine-owned platform, renderer, editor, import, animation, audio, physics, CMake/workflows, dependencies, networking, deployment, release, and R0 untouched.

Delivered increments:
- `GAME-122`: story-gated training unlock after authoritative Lincoln encounter completion.
- `GAME-123`: selectable drill focus, practice pace, and 1..10 attempt target.
- `GAME-124`: consequence-free Endless practice on the existing combat/action/practice owners while preserving persistent loadout state.
- `GAME-125`: bounded multi-attempt run with exact stop or player-defeat debrief, authoritative coaching, challenges, and live timing guidance.
- `GAME-126`: pause/resume for practice-owned combat and incoming-threat clocks.
- `QOL-026`: fixed-capacity rolling damage/hit feedback over a true maximum 20-second activity window.

Research provenance and exact acceptance criteria are in the task packet. Official ZZZ, Genshin Impact, and Granblue Fantasy: Relink material supplies comparator mechanics. QOL-026 comes from a March 2026 Wuthering Waves player discussion and is treated as player preference, not consensus or proof of a current comparator defect.

## Changed paths

Pass-owned diff may contain only:
- `Engine/Scene/ShadowbladeTrainingHub.h`
- `Engine/Scene/LandmarkEncounter.h`
- `Engine/Scene/LandmarkEncounter.cpp`
- `Engine/Scene/LandmarkInteraction.h`, review-required owner witness only
- `Engine/Scene/LandmarkInteraction.cpp`, review-required owner witness only
- `Tests/ShadowbladeTrainingHubPass25Tests.inc`
- `Tests/ShadowbladeTrainingHubPass25ReviewTests.inc`
- `Tests/ThoughtCommandsTests.cpp`, existing registered aggregation only
- `Tasks/GAME-PASS25-SHADOWBLADE-TRAINING-HUB-2026-09-23.md`
- `Docs/Agents/animerpg-hourly/RUN-2026-09-23-PASS25.md`
- `Docs/Agents/animerpg-hourly/STATE.json`

The `LandmarkInteraction` paths were admitted only after independent review proved that encounter completion lacked enough provenance to protect the new training unlock. The existing real interaction path already receives `ShadowbladeActions`; the repair carries that non-owning witness in `LandmarkInteractionReport` so `LandmarkEncounter` can bind the activation combat/action pair without changing engine-owned Win32 code.

## Verification and review history

This PR intentionally retains failing and superseded evidence rather than treating it as green:
- initial head `bdb519591c...`: Windows run `35898246354` / #765 failed Debug link; Release-manifest #544 passed but was invalidated by later repair.
- `2e78d2f8ab...`: Windows #770 and Release-manifest #549 passed, then fresh review found additional retry/timing defects.
- `f15c4a51ab...`: Windows #792 and Release-manifest #571 passed, then fresh review found an exact 20-second cutoff defect.
- `222c544f08...`: Release-manifest #575 passed but Windows #796 failed the existing recent-damage regression, proving the first cutoff repair discarded fresh current-window damage.
- `79f3ed5a23...`: Windows #801 and Release-manifest #580 passed, but exact-head independent review found stale-pending telemetry and encounter-owner provenance defects.
- `8b8165a6f1...`: Windows run `35911658899` / #812 and Release-manifest run `35911658830` / #591 passed, but exact-head independent review found three additional P2 defects, so those green results were invalidated by the required repair.

The current candidate repairs those latest findings:
1. training start, and unlocked encounter retry before hub binding, require the exact combat/action pair that completed the authoritative encounter;
2. `DefenseInput` is validated as exactly Guard or Dodge before the practice session can mutate threat, player, or run state;
3. rolling telemetry keeps the fixed 128-sample, at-most-5-Hz storage contract while each stored sample preserves up to 16 exact damage/hit activity timestamps, so two deltas inside one 200 ms bucket can age out independently at the exact 20-second cutoff. Pathological overflow fails closed instead of silently assigning fresh damage an older timestamp.

Registered regressions cover the review-reported long observation gap with no intermediate flush, exact cutoff expiry, distinct old/fresh deltas inside one stored 200 ms bucket, unrelated defeated combat completion, mismatched action completion, exact-owner completion, unrelated-owner start/retry rejection, invalid defense enum rejection, linked threat-generation timing guidance, frozen debrief telemetry, player-defeat termination/restart, finite-target restoration on retry, stale Ready-state suppression, pause/resume, invalid configuration, and normal bounded completion.

Exact-final-head hosted Windows and Release-manifest gates plus a fresh independent exact-head review remain required after the final state record commit. No final result is claimed until those gates finish. The sandbox clean-clone attempt could not resolve `github.com`, so no local sandbox compile/test success is claimed.

## Counts before final gates

- comparator mechanics researched: **5**
- new community improvements researched: **1**
- increments implemented: **6**
- increments with explicit registered regression coverage: **6**
- increments integrated into existing production game-domain ownership/API: **6**
- native-player-playable verified: **0**
- exact-final-head hosted verified: **0 pending**
- exact-final-head independently reviewed clean: **0 pending**
- merged: **0 pending**

`integrated` does not mean native playable. Current Win32 key/controller input still does not invoke training configure/start/defend/advance/pause. There is no rendered training UI, production training scene, art/audio pass, GPU/performance capture, or cross-process training-state persistence in this packet.

## Next action

Run exact-head hosted Windows Debug/Release tests and Release-manifest integrity, obtain fresh independent review on the same exact head, repair any material finding and rerun invalidated gates, re-read live `main` and PR head plus the full diff, then merge only with expected-head checking. Final workflow IDs, final reviewed SHA, and merge SHA are self-referential and belong in the PR discussion/final task result after those operations.
