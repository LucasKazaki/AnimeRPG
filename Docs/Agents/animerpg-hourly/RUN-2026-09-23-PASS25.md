# AnimeRPG Game Worker Run Receipt: Pass 25

Date: 2026-09-23
Owner: `animerpg-game-hourly`
Packet: `Tasks/GAME-PASS25-SHADOWBLADE-TRAINING-HUB-2026-09-23.md`
Base main at pass start: `4c3308051c910b66dd0aebcf513956926beab505`
Branch: `game/2026-09-23-training-hub-pass25`
Target: `main`
PR: `#44`

## Result prepared for exact-head gates

This bounded pass integrates the previously backend-only Shadowblade training coach into the existing live `LandmarkEncounter` game owner. It does not edit `Engine/Platform`, renderer, editor, import, animation, audio, physics, CMake/workflows, dependencies, networking, deployment, release, R0, Agent Studio, internships, SportsPlay, DZYNE, or another worker's branch/PR.

The six increments are:

- `GAME-122`: Lincoln encounter completion unlocks the training hub, with pre-completion refusal and idempotent live-owner unlock.
- `GAME-123`: atomic selection of coach focus, practice pace, and a 1..10 resolved-attempt target.
- `GAME-124`: consequence-free Endless target practice on the existing combat/Shadowblade/practice owners, preserving persistent loadout selection and binding one exact owner pair.
- `GAME-125`: finite multi-attempt practice flow with automatic next-threat queueing, exact stop or player-defeat debrief, authoritative challenges/coaching, and timing guidance derived from the bound action owner's timing mode.
- `GAME-126`: active-run pause/resume preserving combat time, threat time, player health, and practice progress while paused.
- `QOL-026`: fixed-capacity rolling recent-damage/hit feedback capped at a 20-second window, based on a March 2026 Wuthering Waves player request for better training-dummy rotation feedback.

Research provenance, publication/access dates, counterpoints, adaptations, and player-observable acceptance criteria are in the task packet. The comparator mechanics come from official ZZZ, Genshin Impact, and Granblue Fantasy: Relink material. The QOL source is original player discussion and is explicitly not treated as consensus or proof of a current comparator defect.

## Changed paths

Expected pass-owned diff from the pass-start base:

- `Engine/Scene/ShadowbladeTrainingHub.h` new
- `Engine/Scene/LandmarkEncounter.h` live game-owner integration
- `Engine/Scene/LandmarkEncounter.cpp` live unlock/read-only telemetry plus active-training retry guard
- `Tests/ShadowbladeTrainingHubPass25Tests.inc` new
- `Tests/ThoughtCommandsTests.cpp` existing registered-test aggregation plus a test-only compile include of the exact production `LandmarkEncounter.cpp`, avoiding a shared CMake edit owned by the engine worker
- `Tasks/GAME-PASS25-SHADOWBLADE-TRAINING-HUB-2026-09-23.md` new
- `Docs/Agents/animerpg-hourly/RUN-2026-09-23-PASS25.md` this receipt
- `Docs/Agents/animerpg-hourly/STATE.json` compact state update

Any different changed path is a scope-review failure until explained and admitted.

## Regression coverage added

The registered pass-25 test packet explicitly checks:

- locked-before-completion and live encounter unlock behavior;
- repeated completion idempotency;
- valid and invalid focus/pace/attempt configuration without partial mutation;
- Endless target start, preserved equipped weapon, canonical selected sequence, and active-training encounter-retry refusal;
- exact combat/actions owner binding and mismatched-owner fail-closed behavior;
- four-attempt BossCycle execution, automatic recovery/queue progression, exact debrief stop, and no fifth threat;
- early player defeat ends in Debrief instead of stranding an Active run, and the same owner pair can be reconfigured/restarted afterward;
- timing guidance matches the exact bound action owner's Forgiving timing window rather than a stale hub default;
- coach debrief/timing-guide composition from authoritative session data;
- pause and resume clock/threat/health invariants;
- recent damage/hit accumulation and expiry beyond the 20-second window.

## Review and verification history

Initial PR head `bdb519591c7b1f47bef7e55ed72f89bd771cffae` was not accepted. Windows build/test workflow run `35898246354` / #765 failed in Debug build, while Release-manifest run `35898246615` / #544 passed. The failed build was a real registration defect, not treated as a transient green gate.

Independent Codex review of that exact head found three material issues:

1. P1: `ThoughtCommandsTests` invoked out-of-line `LandmarkEncounter` methods without linking their production implementation.
2. P2: player defeat before the requested attempt cap could strand the hub in `Active` with no queueable threat.
3. P2: feedback timing guidance could remain Standard while the bound `ShadowbladeActions` owner used the existing Forgiving timing preset.

Repairs are now present on this branch: the registered aggregation target compiles the exact production encounter implementation without changing engine-owned CMake; runs end in Debrief on either the configured attempt target or player defeat; and training start captures the authoritative bound action owner's perfect-defense window for timing guidance. New regressions cover the latter two behavioral defects.

Current gate state after these repairs:

- sandbox/Linux clean clone: **not run successfully**. A fresh `git clone` attempt again failed because the sandbox could not resolve `github.com`. No sandbox build/test success is claimed.
- hosted Windows Debug/Release registered build/tests: **pending exact final PR head after this durable record**.
- hosted Release-manifest integrity: **pending exact final PR head after this durable record**.
- native Windows interactive gameplay: **not run, not claimed**.
- GPU/performance capture: **not run, not claimed**.
- independent Codex review: **fresh exact-final-head review required after this durable record**.
- merge: **pending gates**.

The exact final candidate SHA, final workflow run IDs, fresh review outcome, and merge SHA are self-referential to later repository operations. They must be recorded in the PR discussion/final task result after those operations rather than fabricated in this commit.

## Counts at receipt commit

- comparator mechanics researched this pass: **5** minimum, plus Genshin corroboration for bounded difficulty/phase structure
- new community improvements researched: **1**
- increments implemented: **6**
- increments with explicit registered regression coverage: **6**
- increments integrated into an existing production game-domain owner/API: **6**
- native-player-playable verified: **0**
- exact-final-head hosted verified: **0 pending**
- exact-final-head independently reviewed clean: **0 pending**
- merged: **0 pending**

`integrated` here does not mean native playable. The live `LandmarkEncounter::Update` now calls the hub for unlock/read-only sampling and the live encounter owns all training APIs, but current Win32 key/controller input does not call training start/defense/advance/pause. That platform integration remains a separate coordinated dependency.

## Remaining risk and next action

Run exact-head hosted Windows build/tests and Release-manifest integrity, obtain fresh independent Codex review on the same final head, repair every material finding without weakening acceptance, rerun invalidated gates, audit the complete diff and current main/head, then merge only with exact expected-head checking. If all gates pass, the next game pass should prefer an engine-coordinated native input/UI handoff or another genuinely live game-content slice rather than adding another isolated training backend.
